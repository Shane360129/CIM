# CHAT · 親友專屬聊天室 💙

一個部署在 **Cloudflare** 上、介面仿 **LINE** 的網頁聊天室（App 名稱：**CHAT**，程式庫沿用 CIM）。
專為「上班不能用手機、只能開網頁」的情境設計：你在公司用瀏覽器開網頁，
親友在手機把它「加到主畫面」當 App 用，隨時都能聯絡上你。

| | |
|---|---|
| 💬 **1 對 1 與群組聊天** | 訊息永久保存在你自己的 Cloudflare 帳號裡，換裝置、重新整理都不會消失 |
| ✅ **已讀回條** | 跟 LINE 一樣顯示「已讀」（群組顯示「已讀 N」） |
| ⌨️ **即時體驗** | WebSocket 即時推播、「正在輸入…」提示、新訊息通知與音效 |
| 🖼️ **豐富訊息** | 圖片（自動壓縮）、語音訊息、貼圖、**自訂貼圖包**（把家人照片做成貼圖）、表情符號、收回訊息（24 小時內） |
| 💛 **互動** | 表情回應（對訊息按 ❤️👍）、回覆引用、投票、群組公告置頂、訊息搜尋、**連結預覽卡片**（網址自動變超連結，附標題、摘要與縮圖）、未讀分隔線、聊天室置頂 |
| 🔔 **離線推播** | 網頁沒開也收得到通知（Web Push，金鑰全自動產生、零設定） |
| 🍽️ **今天吃什麼** | 從 130＋ 個吃飯類別（乾鍋、海鮮粥、鹹酥雞…）隨機抽一個，也能抽 3 個丟出來給大家投票 |
| 🎮 **聊天室小遊戲** | 八款：圈圈叉叉、四子棋、五子棋、黑白棋、記憶翻翻樂（兩人對戰，兩個座位輪流）、2048、踩地雷（一起玩，輪流接手操作）、猜數字 1A2B（大家搶著猜）。盤面存在伺服器，聊天室成員看到的都是同一盤 |
| 🏠 **聊天室小工具** | 共用待辦清單、行事曆（到時自動提醒所有人），都收在聊天室選單裡 |
| 🌙 **外觀** | 十種風格主題（海洋、和紙抹茶、經典綠、櫻花、墨白、美式復古、日式藍染、手札、薰衣草、夕陽）、十三種字型（基本：粉圓＋黑體、黑體、宋體、楷書、系統字體；繁體手寫：芫荽、辰宇落雁體、清松手寫體、霞鶩漫黑；日系手寫：Klee One、Yomogi、Hachi Maru Pop、Yuji Syuku，缺字自動以芫荽補）、深色模式（自動／手動）、四段字體大小（長輩友善）、上班低調模式 |
| 🔐 **邀請碼註冊** | 只有拿到邀請碼的親友能註冊；第一位註冊者（你）自動成為管理員，可管理成員 |
| 🙈 **成員隱私** | 只有管理員看得到所有帳號；親友只看得到你，以及被你拉進同一個群組的人，彼此無法互相搜尋、私訊或建群 |
| 📝 **我的記事本** | 跟自己聊天，當備忘錄用 |
| 📱 **PWA** | 手機瀏覽器「加入主畫面」後就像原生 App |
| 💸 **免費** | Cloudflare 免費方案即可運行，不需要信用卡 |

---

## 字型來源與授權

字型只在選用時才下載，選擇器裡的預覽只抓幾十個字的子集。

| 字型 | 來源 | 授權 |
|------|------|------|
| 粉圓（Huninn）、芫荽（Iansui）、霞鶩文楷 TC、霞鶩漫黑（LXGW Marker Gothic）、思源黑體／宋體 TC、Klee One、Yomogi、Hachi Maru Pop、Yuji Syuku | Google Fonts（按需載入） | SIL OFL 1.1 |
| 辰宇落雁體 | `public/fonts/chenyuluoyan/`，取自 npm `@fontpkg/chenyuluoyan`，以 fontTools 依 Google Fonts 的繁中分片切成 100 個 woff2 | SIL OFL 1.1（見 `public/fonts/LICENSE-chenyuluoyan.txt`） |
| 清松手寫體 1 | `public/fonts/jason-handwriting/`，取自 npm `@fontpkg/qing-song-shou-xie-ti-1`，同上切成 99 個 woff2 | SIL OFL 1.1（見 `public/fonts/LICENSE-jason-handwriting.txt`） |

自架字型的 CSS 以 `unicode-range` 標示每片涵蓋的字，瀏覽器只會抓畫面上真的出現過的字所在的切片（一般聊天約十片、幾百 KB）。

## 🚀 部署（約 5 分鐘）

兩種方式擇一。共同前提：一個 [Cloudflare 帳號](https://dash.cloudflare.com/sign-up)（免費，不用信用卡）。

> 💡 建議在**自己的電腦**（家裡）部署，公司電腦之後只需要瀏覽器開網址就能用。
> 部署是一次性的動作，之後只有要更新程式時才需要再做。

### 方法 A：用自己的電腦部署

電腦需裝好 [Node.js](https://nodejs.org/)（18 以上）。

```bash
# 1. 下載這個專案
git clone https://github.com/Shane360129/CIM.git
cd CIM

# 2. 安裝工具
npm install

# 3. 登入 Cloudflare（會開瀏覽器讓你授權）
npx wrangler login

# 4. 部署！
npx wrangler deploy
```

Windows 小提醒：如果在 PowerShell 執行 `npm` / `npx` 出現「**因為這個系統上已停用指令碼執行**」，
那是公司或系統的 PowerShell 執行原則擋住了 `.ps1` 捷徑 —— 改開「**命令提示字元（cmd）**」執行
同樣的指令，或把指令改成 `npm.cmd install`、`npx.cmd wrangler deploy` 就能繞過。

### 方法 B：完全用瀏覽器部署（GitHub Actions，零安裝）

公司電腦被限制、不能裝 Node.js？這個方式**從頭到尾只需要瀏覽器**：

1. 登入 [Cloudflare Dash](https://dash.cloudflare.com/) → 進入 **Workers & Pages** 頁一次
   （第一次進入會請你取一個 `*.workers.dev` 子網域名稱，取好就好）。
2. 在 Workers & Pages 首頁右側複製 **Account ID**。
3. 到右上角頭像 → **My Profile → API Tokens** → **Create Token** →
   選 **Edit Cloudflare Workers** 範本 → 建立，複製產生的 token（只會顯示一次）。
4. 回到 GitHub 這個 repo → **Settings → Secrets and variables → Actions** →
   **New repository secret**，新增兩個：
   - 名稱 `CLOUDFLARE_API_TOKEN`，值＝第 3 步的 token
   - 名稱 `CLOUDFLARE_ACCOUNT_ID`，值＝第 2 步的 Account ID
5. 到 repo 的 **Actions** 分頁 → 左側選「**部署到 Cloudflare**」→ **Run workflow**。
   約一分鐘跑完，打開「部署」步驟的紀錄就能看到你的網址。

之後想更新版本，再按一次 Run workflow 即可。

### 部署完成後

你的網址長得像：

```
https://chat.<你的子網域>.workers.dev
```

**不需要**建資料庫、**不需要**設定任何金鑰 —— 所有資料都存在
Worker 附帶的 Durable Object（SQLite）裡；重新部署不會弄丟資料。

---

## 👨‍👩‍👧 開始使用（3 步驟）

1. **你先註冊**：打開網址 → 「註冊」→ 建立帳號。
   第一位註冊的人自動成為**管理員**（在這之前不會有別人能註冊）。
2. **設定邀請碼**：進「設定」→「管理員」→ 輸入一組邀請碼（例如 `family888`）→ 儲存。
   然後點「**複製邀請訊息**」，把網址＋邀請碼傳給親友。
3. **親友註冊**：親友打開網址 → 註冊時輸入邀請碼。
   註冊完系統會**自動幫他們建立與你的聊天室**，一進來就找得到你。

小提醒：

- **公司電腦**：把網頁開著（瀏覽器分頁），有訊息時分頁標題會顯示未讀數
  `(3) CHAT`，也可以開啟桌面通知與音效。
- **親友手機**：用瀏覽器打開網址 → 選單 → 「**加入主畫面**」，
  之後點圖示開啟就是全螢幕 App 體驗。
- **想關閉註冊**：邀請碼清空再按儲存，就沒有人能再註冊（已註冊的不受影響）。
- **成員之間看不到彼此**：親友註冊後，「好友」分頁只會看到你（管理員）。
  你建群把某幾位拉在一起，他們才會互相看到、才能私訊；沒被拉在一起的親友完全不知道對方存在。

### 🍽️🎮✅ 聊天室選單（右上角 ☰）

打開任何一個聊天室，按右上角的 ☰，下面「聊天室功能」就是這四個：

| | |
|---|---|
| 🍽️ **今天吃什麼** | 按「隨機抽一個！」就從 130＋ 個類別裡抽（火鍋、乾鍋、海鮮粥、燒肉、早午餐…），只給大分類不給單一菜名。抽到滿意就「分享到聊天室」；全家意見不合就按「抽 3 個給大家投票」，直接變成一則投票訊息。下面的類別開關可以只留想吃的幾類（例如今天只想吃日式），選擇會記在這台裝置上。 |
| 🎮 **小遊戲** | 八款遊戲選一款就在聊天室開一局，訊息列上直接看得到盤面，成員都能加入。玩到一半想換款式，遊戲視窗下方按「換個遊戲」就好。 |
| ✅ **待辦清單** | 全家共用，誰加的、誰完成的都看得到。 |
| 📅 **行事曆** | 生日、聚餐、繳費提醒，到時間全家都會收到通知。 |

**小遊戲怎麼玩**

**兩人對戰（兩個座位輪流下）**

開局的人自動坐第一個位子，另一位按「加入對戰」坐第二個位子，之後輪流下，沒輪到你的時候格子點不動；
其他成員可以在旁邊看。分完勝負按「再來一局」會換先手，戰績（幾勝幾負）一直累積。

| 遊戲 | 玩法 |
|---|---|
| ⭕ **圈圈叉叉** | 3×3，連成一線就贏 |
| 🔴 **四子棋** | 7×6，點直行投下棋子，先連成四顆（橫、直、斜）就贏 |
| ⚫ **五子棋** | 13 路棋盤，先連成五顆就贏 |
| 🔳 **黑白棋** | 8×8，只能下在夾得到對方棋子的地方，夾住就翻面；沒地方下自動跳過，最後子多的贏。輪到你時可以下的點會標出來 |
| 🃏 **記憶翻翻樂** | 20 張牌配 10 對，翻兩張，配對成功收下並再翻一次；翻錯換人。蓋著的牌伺服器不會送牌面，看封包也偷不到 |

五子棋、黑白棋格子小，聊天泡泡裡只顯示戰況，按「放大遊玩」開大盤面才能下。

**大家一起玩**

| 遊戲 | 玩法 |
|---|---|
| 🔢 **2048** | 共用一盤，但**同一時間只有一位「操作者」**能動 —— 按「我要玩」接手、「換人玩」放開，別人才接得到（操作者閒置超過 1 分鐘可直接接手，避免卡住）。方向鍵、畫面按鈕或直接滑動都可以，分數大家一起累積 |
| 💣 **踩地雷** | 9×9 十顆雷，同樣是輪流接手操作。數字代表周圍有幾顆雷，懷疑有雷就開「插旗模式」標起來；第一下保證不會踩到雷，全部排完會記下最佳秒數 |
| 🔡 **猜數字 1A2B** | 不用排隊，聊天室裡誰都能猜。系統出一組 4 位不重複數字，每次回報 A（數字位置都對）與 B（數字對位置錯），猜過的紀錄大家都看得到，誰先猜中就是誰贏 |

---

## 🧱 技術架構

```
瀏覽器（前端 SPA，public/）
   │  HTTPS（REST API：登入、傳訊息、已讀…）
   │  WebSocket（即時接收：新訊息、已讀、輸入中…）
   ▼
Cloudflare Worker（src/index.js）── 靜態檔由 Cloudflare Assets 供應
   ▼
ChatServer Durable Object（src/chatserver.js）
   ├─ SQLite 儲存：使用者、聊天室、訊息、已讀進度、邀請碼、遊戲盤面
   └─ WebSocket Hibernation：所有線上成員的即時連線
```

設計重點：

- **單一 Durable Object 實例**承載全部狀態。親友規模（數十人）下這是最簡單、
  最一致的架構：沒有分散式競態、免設定 D1/KV/R2、部署零設定。
- **密碼**以 PBKDF2-SHA256（10 萬次迭代＋隨機鹽，為 Cloudflare 執行環境上限）雜湊儲存；登入與邀請碼嘗試皆有次數鎖定。
- **Session** 為隨機 token 存伺服器端，60 天未使用自動失效；改密碼會踢掉其他裝置。
- **成員可見性在伺服器端強制**：`/api/users`、建立私訊／群組、邀請進群、以及成員資料的即時推播，
  都以「管理員 or 同聊天室」規則過濾，不是只靠前端隱藏。
- WebSocket 使用 **Hibernation API**，沒人講話時 DO 休眠，幾乎不耗用量。
- 圖片在**瀏覽器端壓縮**成 JPEG（最長邊 1280px、約 450KB 內）後以 data URL 存進資料庫。
- **小遊戲的盤面與規則都在伺服器端**（`games` 資料表，一則遊戲訊息一列）：
  誰能下、輪到誰、有沒有連成線、黑白棋夾到哪些子、2048 要在哪裡長出新數字，
  全部由 Durable Object 判定後廣播，前端只負責畫。所以兩邊看到的一定是同一盤，
  也沒辦法從瀏覽器改盤面作弊。
- **會藏資訊的遊戲另外過濾一層**：記憶翻翻樂沒翻開的牌面、踩地雷的雷區、猜數字的答案，
  都不會出現在送給前端的狀態裡（`publicGame()`），所以直接看 WebSocket 封包也偷不到答案。

### 免費方案夠用嗎？

夠。Workers 免費方案每天 10 萬次請求、Durable Objects（SQLite 版）含 5GB 儲存。
以家人朋友的訊息量（即使天天貼圖傳照片）通常連免費額度的邊都碰不到。

---

## 🛠️ 常用操作

| 想做什麼 | 指令／位置 |
|---|---|
| 本機開發預覽 | `npm run dev` → http://localhost:8787 |
| 跑 API 煙霧測試 | 另開終端機 `npm test`（驗證隱私規則、權限、核心流程與小遊戲動作；需先 `npm run dev`，建議用乾淨資料 `rm -rf .wrangler/state`） |
| 跑小遊戲規則測試 | `npm run test:games`（2048 合併與死局、連線判定、黑白棋夾翻、1A2B 的 A／B、踩地雷佈雷；不需要啟動伺服器） |
| 部署／更新 | `npx wrangler deploy` |
| 重新產生 App 圖示 | `npm run icons`（改 `scripts/gen-icons.mjs` 配色後執行） |
| 備份聊天資料 | 網頁「設定」→「管理員」→「下載聊天備份」 |
| 換自己的網域 | Cloudflare Dash → Workers → chat → Settings → Domains & Routes |

## 📁 檔案結構

```
wrangler.jsonc        Cloudflare 設定（Worker、Durable Object、靜態資源）
src/index.js          Worker 入口（路由轉送）
src/chatserver.js     聊天伺服器：帳號、訊息、群組、WebSocket（Durable Object）
public/index.html     前端頁面骨架
public/app.js         前端邏輯（登入、聊天、已讀、通知、貼圖…）
public/style.css      LINE 風格樣式（全尺寸響應式：小手機／手機／平板／桌面／超寬螢幕）
public/manifest.webmanifest + sw.js + icons/   PWA（加到主畫面）
scripts/gen-icons.mjs 產生 PNG 圖示的小工具（零相依）
scripts/test-api.mjs  API 煙霧測試（成員可見性、權限、登入、訊息、小遊戲）
scripts/test-games.mjs 小遊戲規則單元測試（2048、連線判定、黑白棋、1A2B、踩地雷）
legacy-github-pages/  舊版（GitHub Pages + MQTT 端對端加密版）留存
```

## 🔔 離線推播小抄

在「設定 → 通知 → 離線推播」開啟後，網頁完全關閉也會收到「新訊息」提醒
（內容一律不顯示聊天文字，兼顧隱私與低調）：

- **Android／電腦 Chrome、Edge**：直接開啟即可。
- **iPhone**：iOS 16.4 以上，需先用 Safari「**加入主畫面**」，再從主畫面圖示開啟 CHAT，
  才能開啟離線推播（這是 Apple 的限制）。
- 為避免轟炸，離線推播每人最多每分鐘一則。

## ⚠️ 已知限制

- 沒有語音／視訊通話。
- 傳一般檔案（文件、影片）尚未支援（適合之後掛 Cloudflare R2 再加）。
- 訊息在伺服器上以明文儲存於**你自己**的 Cloudflare 帳號（跟 LINE 一樣是伺服器可讀的模式）；
  若需要端對端加密，`legacy-github-pages/` 裡的舊版是加密的（但無帳號、無歷史訊息）。
- 請遵守你所在公司的網路使用規範。
